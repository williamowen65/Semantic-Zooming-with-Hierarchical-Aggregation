// Legacy placeholder. Hierarchy-wide overview zoom was removed in favor of
// directly controlling each layer's height, so no zoom-specific gap correction
// is needed anymore.

// Layer-kind click behavior is loaded by layer-kind-toggle.js with the rest of
// the layer-kind modules so there is a single, cache-busted source of truth.
