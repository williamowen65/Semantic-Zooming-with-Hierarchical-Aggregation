# Profiles and Identity — Domain Model and UML

## Known Context

A profile can expose multiple root Nodes. Root status is contextual rather than requiring a separate RootNode class. Authentication and identity belong to the authoritative application layer.

## UML

_To be designed. Likely concepts include User/Account, Profile, and an association between a Profile and zero or more root Nodes._
